package BillboardViewer.Requests;

import BillboardViewer.BillboardViewer;

/**
 * An ENUM storing the request types that the billboard viewer could make
 */
public enum ViewerRequestType {
    getXML
}
